This project is a datastructure with the operations of a hash.  However, the way the data is stored is more like a semi-balanced tree. If two keys have a common prefix, the nodes storing data for those keys will share a common ancestor.